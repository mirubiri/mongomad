/ - content_for :image
/  = object_main_image_tag(item,100,100)

/ - content_for :name
/  = item.name

/ - content_for :controls
/  = link_to 'delete', item_path(item.id), method: :delete, confirm: "Are you sure?"
/  = link_to 'edit', edit_item_path(item.id)

/ El cash no tiene nombre ni los controles "delete y edit", asi que esto no puede ir aqui
