.avatar
  .image
    = yield_content :image

  .user_data
    .full_name
      = data.visited_user.full_name

    .location
      | location

    .reputation
      .icon
         = static_image_tag('star')
      .value
        | 75%

    .friends
      .icon
        = static_image_tag('friends')
      .number
        | 350
