- content_for :composer_goods
   - proposal.composer_goods.each do |good|
    - if good._type == 'Cash'
      = element :good, view: :cash_big, data:good
    - else
      = element :good, view: :product_big, data:good

- content_for :receiver_goods
   - proposal.receiver_goods.each do |good|
    - if good._type == 'Cash'
      = element :good, view: :cash_small, data:good
    - else
      = element :good, view: :product_small, data:good
