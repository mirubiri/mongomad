var url = this.href;

$("#ppal_container").html('<%= escape_javascript(render @negotiations) %>');

alert('funciona');

igualaAlturaUserContainerMainContainer();
maximizaAlturaConversacion();

history.pushState({path:url},"", url);