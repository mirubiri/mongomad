var negotiation_id = "<%= @negotiation_id(negotiation) %>"
$("#"+ negotiation_id).find(".conversation").html("<%= escape_javascript(render :partial => 'negotiations/partials/conversation', :locals => { :negotiation => @negotiation }).html_safe %>");
$("#"+ negotiation_id).find(".message").html('<input type="text" placeholder="write here" name="text">');
$("#"+ negotiation_id).find("input").focus();