//Quito la ventana modal
$('#edit-negotiation-form').modal('hide');

//Cargo en el contenedor principal los datos nuevos y destruyo el masonry.
$('#edit-negotiation-form').on('hidden', function () {
  $('#ppal_container')
  .masonry('destroy')
  .html("<%= escape_javascript(render :partial => 'negotiations/partials/negotiation').html_safe %>");
  
  //Elimino la ventana modal
  delete $(this);
})

masonryOffer();